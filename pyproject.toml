# This file is used to package the CurrencyexchangerateatehostPlugin plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.13.3
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-currency-exchangerateate--host-plugin"
description = "Fetch exchange rate from exchangerate.host"
dynamic = ["version"]
authors = [
    { name = "Jimmy Cheng",email = "jimmy@kingyoung.com.tw" },
]
readme = "README.md"
license = "MIT"
license-files = ["LICENSE", "NOTICE"]
keywords = ["inventree", "plugin", "currency", "exchange-rate", "exchangerate.host"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://github.com/jimmyken793/currency-exchangerate-host-plugin"
Repository = "https://github.com/jimmyken793/currency-exchangerate-host-plugin"

[project.entry-points."inventree_plugins"]
CurrencyexchangerateatehostPlugin = "currency_exchangerateate__host_plugin.core:CurrencyexchangerateatehostPlugin"

[tool.setuptools]
packages = ["currency_exchangerateate__host_plugin"]
include-package-data = false

[tool.setuptools.dynamic]
version = {attr = "currency_exchangerateate__host_plugin.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["currency_exchangerateate__host_plugin"]
